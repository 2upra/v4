// task.js

/**
 * Función auxiliar para reiniciar una tarea y sus subtareas.
 * Asume que window.reiniciarPost(id, tipo) está disponible.
 * @param {string} idTareaPrincipal El ID de la tarea a reiniciar (potencialmente padre).
 */
// This comment was the only thing remaining after the previous refactoring.
// It seems to be a leftover comment that was not associated with any specific function that was moved.
// I will keep it as per the instructions "The file can still contain comments".
